<form method="post" action="{{ path('app_ice_cream_delete', {'id': ice_cream.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ice_cream.id) }}">
    <button class="btn">Delete</button>
</form>
